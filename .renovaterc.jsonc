{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "assignees": ["davide"],
  "extends": [
    "config:best-practices",
    "customManagers:dockerfileVersions",
    "customManagers:biomeVersions",
    ":semanticCommits"
  ],
  "packageRules": [
    // Automerge actions and devcontainer deps.
    {
      "automerge": true,
      "groupName": "CI dependencies",
      "groupSlug": "ci",
      "matchFileNames": [".devcontainer/**", ".forgejo/**", "ci.Dockerfile", "cups.Dockerfile"],
      "matchUpdateTypes": ["!major"]
    },
    {
      "automerge": true,
      "groupName": "CI dependencies",
      "groupSlug": "ci",
      "matchPackageNames": ["git.zarantonello.dev/projects/events-cash-register-ci"]
    },
    // Formatters and linters get automerged.
    {
      "automerge": true,
      "groupName": "CI dependencies",
      "groupSlug": "ci",
      "matchPackageNames": ["svelte-check", "@biomejs/biome", "@types/*", "prettier*", "pnpm", "vitest", "turbo"],
      "matchUpdateTypes": ["!major"]
    },
    // These are devDependencies but they can cause user visible changes.
    {
      "matchPackageNames": [
        "@sveltejs/adapter-node",
        "@sveltejs/enhanced-img",
        "@sveltejs/kit",
        "@tailwindcss/vite",
        "bits-ui",
        "phospor-svelte",
        "svelte",
        "tailwindcss",
        "vite"
      ],
      "matchUpdateTypes": ["!major"],
      "semanticCommitType": "fix"
    },
    // Like above but for major versions we use feat.
    {
      "matchPackageNames": [
        "@sveltejs/adapter-node",
        "@sveltejs/enhanced-img",
        "@sveltejs/kit",
        "@tailwindcss/vite",
        "bits-ui",
        "phospor-svelte",
        "svelte",
        "tailwindcss",
        "vite"
      ],
      "matchUpdateTypes": ["major"],
      "semanticCommitType": "feat"
    },
    {
      "automerge": true,
      "matchPackageNames": ["git.zarantonello.dev/igl/website"]
    }
  ],
  // Raise limit or there are situations where we can't keep up with the number
  // of deps to update.
  "prHourlyLimit": 6
}
