{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": { "outputs": ["./dist", "./build/cups.node"] },
    "format:check": { "dependsOn": ["setup"] },
    "format:fix": { "dependsOn": ["setup"] },
    "lint:check": { "dependsOn": ["setup"] },
    "lint:fix": { "dependsOn": ["setup"] }
  }
}
